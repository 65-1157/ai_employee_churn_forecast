## What changed

## Why

## Checklist
- [ ] Tests pass locally (`pytest tests/`)
- [ ] If this touches `schema_registry/` or `src/schema_mapper.py`, mapping rules were reviewed
- [ ] If this touches `src/ensemble.py`, weight-floor and significance-gate logic still hold
